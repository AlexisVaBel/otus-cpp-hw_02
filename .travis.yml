 dist: xenial
 language: cpp
 compiler: clang
 before_script:
         - sudo apt update
         - sudo apt install libspdlog-dev -y
         - sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 379CE192D401AB61
 script:
         - cmake .
         - cmake --build .
         - cmake --build . --target package
 deploy:
         provider: script
         skip_cleanup: true
         script:
           - curl -T ip_filter-0.0.$TRAVIS_BUILD_NUMBER-Linux.deb -ualexisvabel:$BINTRAY_API_KEY "https://api.bintray.com/content/alexisvabel/otus-cpp/homework/$TRAVIS_BUILD_NUMBER/home/travis/build/AlexisVaBel/otus-cpp-hw_02/ip_filter-0.0.$TRAVIS_BUILD_NUMBER-Linux.deb;deb_distribution=xenial;deb_component=main;deb_architecture=amd64;publish=1"
